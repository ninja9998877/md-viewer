# 更新日志

## [未发布]

### 新增

- **开源** — 本仓库转为公开，MIT。桌面版免费使用，手机版另作商店发行。
- **菜单里的「iPhone 版」** — 指向 App Store 上架的 iPhone 版。链接带 `/cn/`，
  因为不带地区前缀的 `apps.apple.com/app/id…` 会被重定向到 App Store 的 Today 首页，
  而不是 App 页面（浏览器实测；普通 HTTP 请求看不出这个问题）。
  和「反馈问题」一样是交给系统浏览器打开，App 自己不联网。
- **Issue 模板** — 功能出错 / 读起来不舒服两类，空白 issue 关闭。
- **文内搜索** — `Ctrl/Cmd+F` 或菜单 →「查找」。命中计数、上下跳转、Esc 关闭。
  匹配在**源文本**上算，所以虚拟列表还没挂载的命中同样能找到。
- **阅读位置记忆** — 按文件记住读到哪儿，重新打开回到原位（实测锚点漂移 0.0px）。
- **图片受控放大** — 点图片进全屏浮层：双击缩放、滚轮缩放、拖动平移、点背景或
  Esc 退出。
- **读剪贴板** — 菜单 →「读剪贴板」，直接打开剪贴板里的 Markdown，绕开文件系统。
- **`path:line` 引用可点击** — `src/App.tsx:190` 交给系统打开（编辑器），
  这是引用的意义所在。
- **diff 代码块按行上色** — 加绿、减红，`@@` 与文件头弱化。
- **代码块显示文件名** — ```` ```ts title="src/App.tsx" ```` 与裸路径写法都认。
- **菜单显示版本号**与**「反馈问题」** — 把诊断信息（版本 / 运行时 / 行列数 /
  最后一次错误）复制到剪贴板，再打开仓库的新建 issue 页；剪贴板不可用时改为
  把报告显示出来，并且**不跳转**。
- **五种界面语言** — 简体中文 / 繁體中文 / English / 日本語 / Deutsch，
  按 `navigator.languages` 的首选顺序匹配。
- **最近列表按住删除** — 按住（右键同样）后出现确认行，删除键为红色。
- **应用内对话框** — 取代 `window.alert/confirm`。
- **单元测试** — vitest，51 项；新增 CI（类型检查 + 测试 + 前端构建），
  贡献者不必装 Tauri 工具链就能验证改动。

### 变更

- **分享脚注**写明两个卖点：专为 Agent 设计 · 纯本地，不联网。
- 打开文件增加 20 秒期限，超时给出明确提示。
- 外部链接交给系统浏览器打开 —— 该联网的是浏览器，不是墨页。
- 移动端顶栏压缩为两行（本仓库不受影响，两仓共用组件）。

### 修复

- **最近列表在授权失效后点不开** — `content://` 与安全作用域 `file://` 的授权
  只活在接收它的进程里，而列表能挺过去。现在读取成功时留存本地快照并回落。
- **滑到底部反复回弹** — mermaid 空占位高度被当作真实高度记录，以及末尾逐段
  补偿不收敛。
- **文件打不开时的沉默** — 补上 pending 标记与明确的失败提示。
- **双击 .md 打开失败时毫无提示** — 启动路径只写了 `console.error`，看起来
  就像「双击没反应」。现在与其他打开路径一样给出明确提示。
- **快速连续打开两个文件会显示错的那个** — 读取没有序号守卫时，先发起的慢读
  取会后落地并覆盖后发起的快读。
- **快捷键 `E` 与顶栏按钮行为不一致** — 两者本是两份代码，按钮会置上的「编辑器
  驱动预览」标记快捷键漏了。现在共用同一条路径。
- **语言检测选错语种** — 原来用 `.some()` 扫 `navigator.languages`，只要列表里
  出现过 `zh` 就选中文、否则一律英文，`["ja-JP", "en-GB"]` 会被判成英文；
  现在按首选顺序遍历，并区分 `zh-TW/HK/MO` 与 `zh-CN/SG`。
- **占位文档被当成真文档** — 欢迎页判定只比对中英两套文案，新增语言后既不认
  自己的欢迎页（可对其「保存」），切换语言也不重新翻译。现在覆盖全部词条。

### 隐私

- 无遥测。诊断信息只在主动点击时生成并写入剪贴板，不发送到任何地方。
