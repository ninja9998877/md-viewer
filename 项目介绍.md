# 墨页 Moye

**给 AI 写的 Markdown 用的本地阅读器。打开就是预览，像纸一样读，文件不离开你的电脑。**

---

## 项目目标

Agent 每天输出大量 `.md`。市面上的工具几乎都是编辑器：Typora、Obsidian、VS Code 预览。人要的是把长文读完，而不是先面对一个输入框。

**墨页的设计目标是：**

- **默认就是干净的预览界面**，让 AI 长文像一篇读物
- **为 Agent 文档排版**（提示块、目录、代码折叠、公式、图表）
- **本地隐私**，文件不出本机
- **编辑为辅**，需要时再按 `E`
- **双击 `.md` 即可打开**，类似阅读器而不是 IDE

---

## 核心设计理念

### 1. 默认预览模式（干净优先）

- 软件启动后默认进入**纯预览界面**
- 编辑功能被隐藏，只有在需要时才显示
- 提供极低干扰的阅读体验

### 2. 编辑入口极简

支持多种极简方式进入编辑：
- 右下角浮动“编辑”按钮
- 按 `E` 键快速切换
- 双击预览区域进入编辑
- 按 `Esc` 快速回到预览

### 3. 文件操作行为

| 操作                  | 默认模式   | 说明                              |
|-----------------------|------------|-----------------------------------|
| 双击 `.md` 文件打开   | **预览模式** | 类似阅读器，干净展示              |
| 软件内点击「新建」     | **编辑模式** | 快速开始写作                      |
| 拖拽文件到窗口         | **预览模式** | 方便快速查看                      |
| 按 `E` / 双击预览区    | 进入编辑   | 按需编辑                          |

### 4. 完整语法支持

- GitHub Flavored Markdown（表格、任务列表、删除线、自动链接等）
- 数学公式（KaTeX）
- Mermaid 图表（流程图、时序图、甘特图等）
- 代码高亮（多种语言）
- 图片、引用、脚注、嵌套列表等

---

## 技术栈

- **后端**：Tauri 2（Rust）—— 体积小、性能高、原生窗口
- **前端**：React 19 + TypeScript + Tailwind CSS
- **编辑器**：CodeMirror 6 —— 大文件性能优秀
- **渲染引擎**：react-markdown + remark/rehype 生态
  - 数学公式：KaTeX
  - 图表：Mermaid
  - 代码高亮：rehype-highlight
- **文件操作**：Tauri FS + Dialog 插件

---

## 使用方式

### 普通使用

1. 安装后双击任意 `.md` 文件即可打开（默认预览模式）
2. 软件内点击“新建”可快速创建新文档（默认编辑模式）
3. 支持拖拽文件快速打开

### 远程使用（SSH）

本项目特别适合通过 SSH 远程操作：

```bash
# 通过 SSH 登录到运行墨页的电脑
ssh user@your-pc

# 直接使用终端命令打开或编辑 Markdown
# （后续可扩展为命令行工具）
```

---

## 项目状态

当前处于活跃开发阶段，已实现核心功能：

- [x] 双栏编辑 + 实时预览
- [x] 完整 Markdown 语法渲染
- [x] 默认预览模式 + 极简编辑入口
- [x] 基础文件操作（新建、打开、保存）
- [x] 暗色/浅色主题
- [x] 键盘快捷键支持
- [x] 拖拽打开文件
- [ ] 文件关联安装包（开发中）
- [ ] 导出功能
- [ ] 目录大纲
- [ ] 图片拖拽插入

---

## 开发初衷

作者在日常工作中需要频繁阅读和编写大量 Markdown 文档，市面上现有的工具要么功能冗余、要么性能不佳、要么界面不够克制。因此决定自己做一个**够用、够快、够干净**的 Markdown 工具。

---

## 后续规划

- 更强的文件关联体验（安装后双击直接打开）
- 导出为 PDF / HTML
- 目录大纲侧边栏
- 多标签页支持
- 自定义主题与 CSS
- 命令行工具支持（便于 SSH 远程调用）

---

**欢迎提出建议和需求！**

项目根目录下可直接运行：

```bash
npm run tauri dev
```

或使用已打包的版本进行测试。
